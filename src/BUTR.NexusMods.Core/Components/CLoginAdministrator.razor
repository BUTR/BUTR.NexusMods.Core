@inject NavigationManager _navigationManager
@inject ITokenContainer _tokenContainer
@inject IAuthenticationProvider _authenticationProvider

<!--https://jsfiddle.net/bootstrapious/j80aheo9/-->
<div class="container">
    <div class="row">
        <div class="col-lg-10 col-xl-7 mx-auto">
            <div class="card">
                <h4 class="card-header text-center">Administrator Log In</h4>
                <div class="card-body">
                    <EditForm Model="@_model" OnValidSubmit="OnValidSubmit">
                        <DataAnnotationsValidator />
                        <div class="mb-3">
                            <label for="inputToken" class="form-label">Administrator Token</label>
                            <InputText id="inputToken" @bind="_model.Input" type="password" required="" class="form-control border-0 shadow-sm px-4 text-primary" />
                        </div>
                        <button type="submit" class="btn btn-primary text-uppercase mb-2" disabled="@_isLoading">
                            @if (_isLoading)
                            {
                                <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                            }
                            Login
                        </button>
                    </EditForm>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    private readonly LoginModel _model = new();
    private bool _isLoading;

    private async void OnValidSubmit()
    {
        _isLoading = true;

        try
        {
            if (await _authenticationProvider.AuthenticateAsync(_model.Input, "admin") is not { } result)
            {
                _isLoading = false;
                StateHasChanged();
            }
            else
            {
                await _tokenContainer.SetTokenAsync(result);
                _navigationManager.NavigateTo(_navigationManager.QueryString("returnUrl") ?? "", true);
            }
        }
        catch
        {
            _isLoading = true;
            StateHasChanged();
        }
    }
}